import React  from 'react'

export default function Uselayout() {
  return (
    <>
     {/* uselayout works similar to useefect,but it is called before the user interfae mounted.   */}
     {/* useefect ge    ts called after pprintig the DOM element.*/}
     {/* useayout gets called before the printing the DOm element.*/}
     {/* Reaining this point: */}
    </>
  )
}
